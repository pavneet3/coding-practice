package prc.msl;

public class MedianTwoSortedArrays {
	public double findMedianSortedArrays(int[] a, int[] b) {
		int t = (a.length * b.length) / 2;
		int ai = a.length / 2;
		int bi = ;
		while ((ai == -1 || bi == -1) && a[ai] > b[bi + 1] && a[ai + 1] > b[bi]) {
			
		}
	}
}
